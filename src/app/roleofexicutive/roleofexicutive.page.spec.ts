import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RoleofexicutivePage } from './roleofexicutive.page';

describe('RoleofexicutivePage', () => {
  let component: RoleofexicutivePage;
  let fixture: ComponentFixture<RoleofexicutivePage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(RoleofexicutivePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
