import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ViewroleofexicutivePage } from './viewroleofexicutive.page';

describe('ViewroleofexicutivePage', () => {
  let component: ViewroleofexicutivePage;
  let fixture: ComponentFixture<ViewroleofexicutivePage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ViewroleofexicutivePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
