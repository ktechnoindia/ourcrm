import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Ccstep4Page } from './ccstep4.page';

describe('Ccstep4Page', () => {
  let component: Ccstep4Page;
  let fixture: ComponentFixture<Ccstep4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Ccstep4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
